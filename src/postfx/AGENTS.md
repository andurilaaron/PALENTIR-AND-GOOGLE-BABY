# Lane: src/postfx/** + src/ui/PostFxPanel.tsx

Allowed:
- Edit/create files under src/postfx/** and src/ui/PostFxPanel.tsx only.

Not allowed:
- Editing outside these paths — no touching core, layers, or config files.

If you think you need to touch anything outside this lane:
- STOP and write why in the handoff note, then request permission in the chat.
