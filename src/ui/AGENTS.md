# Lane: src/ui/**

Allowed:
- Edit/create files under src/ui/** only.

Not allowed:
- Editing outside src/ui/** (including src/core/**) unless the prompt explicitly allows it.

If you think you need to touch anything outside this lane:
- STOP and write why in the handoff note, then request permission in the chat.
