# Lane: src/core/**

Allowed:
- Edit/create files under src/core/** only.

Not allowed:
- Editing outside src/core/** (including src/ui/**) unless the prompt explicitly allows it.

If you think you need to touch anything outside this lane:
- STOP and write why in the handoff note, then request permission in the chat.
